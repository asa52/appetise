[project]
name = "appetise"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pre-commit>=4.5.0",
    "pydantic>=2.12.5",
    "pint>=0.23",
    "pytest>=8.0.0",
]
